import { OutletContext } from "../datatypes/datatypes";

const NoteForm = ({ notes, currentNote, setCurrentNote }: OutletContext) => {
  console.log(notes);
  return <div>NoteForm</div>;
};

export default NoteForm;
